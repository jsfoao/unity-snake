namespace UnityEngine
{
    public enum Direction { Left, Right, Down, Up }
    public enum ObjectType { None, Fruit, Body, Loot }
    public enum CollisionType { Passive, Active }
    public enum AIState { Wander, Focus }
}
